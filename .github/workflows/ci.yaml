name: "CI"
on:
  push:
    
jobs:
  ci:
    name: "Run CI"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dominikh/staticcheck-action@v1.4.1
        with:
          version: latest
